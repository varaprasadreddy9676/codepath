{
  "_comment": "Copy this into your project's .cursor/mcp.json or global Cursor MCP settings. Update the command path to your codepath binary location.",
  "mcpServers": {
    "codepath": {
      "command": "/path/to/codepath/target/release/codepath-mcp",
      "env": {
        "CODEPATH_API_URL": "http://localhost:3000"
      }
    }
  }
}
